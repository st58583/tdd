apiVersion: v1
kind: Service
metadata:
  name: rental-service
spec:
  type: NodePort
  selector:
    app: rental-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080